# Data

The notebooks use MNIST through `torchvision.datasets.MNIST`.

The dataset is downloaded automatically when the notebook is run with `download=True`.

Large generated datasets, model checkpoints, and intermediate outputs should not be committed to GitHub unless they are intentionally selected portfolio artifacts.
